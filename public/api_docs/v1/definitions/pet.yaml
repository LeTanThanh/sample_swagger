type: "object"
required:
  - name
  - photoUrls
properties:
  id:
    type: "integer"
    example: 1
  category:
    $ref: "#/definitions/Category"
  name:
    type: "string"
    example: "Golden"
  status:
    type: "string"
    enum:
    - available
    - pending
    - sold
    example: pending
